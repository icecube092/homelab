map $ssl_preread_server_name $backend {
    cloud.{{nginx.domain}}  127.0.0.1:8443; # vpn
    default                    127.0.0.1:4443;
}

server {
    listen 443;
    listen [::]:443;
    ssl_preread on;
    proxy_pass $backend;

    proxy_timeout 3600s;
    proxy_connect_timeout 5s;
}

